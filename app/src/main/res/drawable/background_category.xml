<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#EC3977"/>

    <!-- Bo góc -->
    <corners android:radius="20dp"/>

    <!-- Viền (stroke) -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF"/>
</shape>
